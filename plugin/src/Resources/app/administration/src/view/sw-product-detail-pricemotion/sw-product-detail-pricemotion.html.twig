<sw-card>
    <iframe
        v-if="url"
        ref="iframe"
        :src="url"
        :style="{
            border: 0,
            width: '100%',
            height: `${iframeHeight}px`
        }"
    ></iframe>
</sw-card>