#!/usr/bin/env bash

set -euo pipefail
cd "$(dirname "$0")/.."

export PROJECT_ROOT="$PWD/shopware"

(
	cd "$PROJECT_ROOT/vendor/shopware/administration/Resources/app/administration"
	npm run build
)

"$PROJECT_ROOT/bin/console" asset:install
